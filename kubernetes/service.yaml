apiVersion: v1
kind: Service
metadata:
  name: sudokupro
  labels:
    app: sudokupro
spec:
  type: ClusterIP
  selector:
    app: sudokupro
  ports:
    - name: http
      port: 80
      targetPort: 8080
